project/
├── index.js
├── .env
├── bots.json
└── package.json

El index.js es uno solo ejecutandose se agrega y se quita en el bots.js pero seria bueno para controlar mejor meterlo en base de datos y un comando para regenerar el bots.json
# agent-engine-telegram
